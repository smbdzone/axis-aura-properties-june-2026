services:
  backend:
    build:
      context: ./axis-aura-Back-End-june-2026
      dockerfile: Dockerfile
    container_name: axis-aura-backend
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./axis-aura-Back-End-june-2026/.env
    environment:
      HOST: 0.0.0.0
      PORT: 5000
      HOTSPOT_FRONTEND_URL: http://localhost:3000
    volumes:
      - backend-uploads:/app/uploads
    networks:
      - axis-aura-net

  dashboard:
    build:
      context: ./axis-aura-properities-dashbaord-june-2026
      dockerfile: Dockerfile
    container_name: axis-aura-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:5000
    depends_on:
      - backend
    networks:
      - axis-aura-net

volumes:
  backend-uploads:

networks:
  axis-aura-net:
    driver: bridge
